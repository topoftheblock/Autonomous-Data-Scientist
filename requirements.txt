# requirements.txt
# Core dependencies for the Autonomous Data Science Agent

# LLM & Agent framework
# Pinned below 1.0: langchain 1.x removed the legacy `langchain.agents`
# (AgentExecutor / create_openai_tools_agent) API this project's executor
# node depends on. Migrating to the new `create_agent` API is tracked as a
# roadmap item, not done here.
langchain>=0.3,<1.0
langchain-openai>=0.2,<1.0
langgraph>=0.2,<1.0

# Data science stack
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pyarrow>=14.0.0  # parquet read/write support for pandas

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Sandbox (required for the execute_python tool)
RestrictedPython>=7.0

# Environment & configuration
python-dotenv>=1.0.0
pyyaml>=6.0  # for config/config.yaml

# OpenAI API (langchain-openai will pull it, but explicit prevents surprises)
openai>=1.0.0

# Testing
pytest>=7.0.0